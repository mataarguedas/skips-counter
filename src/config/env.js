// Environment variable config.
// VITE_GOOGLE_CLIENT_ID has been removed — OAuth is no longer used.
// Environment variables will be reintroduced here when production
// Google Workspace OAuth is configured for the service-account approach.
export const ENV = {};
